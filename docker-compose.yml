version: '2'

services:
  payments_service:
    build: .
    container_name: payments_service_v2_1
    networks:
      - servers_external_network
    ports:
      - "82:3000"
    env_file:
      - ./.env
    tty: true
    volumes:
      - .:/src
      - .:/usr/src/app
      - ./bin/log/payments_service_v2:/var/log/application/payments_service_v2
    # depends_on:
    #   - redis

  # redis:
  #   image: redis:7-alpine
  #   container_name: payments_redis
  #   networks:
  #     - servers_external_network
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   command: redis-server --appendonly yes
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 30s
  #     timeout: 3s
  #     retries: 3

# volumes:
#   redis_data:

networks:
  servers_external_network:
    external: true